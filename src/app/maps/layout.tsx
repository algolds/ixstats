import { type ReactNode } from 'react';

export const metadata = {
  title: 'Maps - IxStats',
  description: 'Explore IxEarth with Google Maps-style interface',
};

export default function MapsLayout({ children }: { children: ReactNode }) {
  return children;
}
