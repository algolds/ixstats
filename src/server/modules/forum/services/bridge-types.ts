/**
 * @deprecated Use `src/server/shared/bridge-types.ts` instead.
 * Retained as temporary compatibility re-export for Plan 158.
 */

export type {
  BridgeMessage,
  BridgeSyncResult,
  BridgeAdapter,
} from "~/server/shared/bridge-types";
