# PostgreSQL Client Authentication Configuration File
# SECURITY HARDENED - January 2026
#
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (inside container) - trust for maintenance
local   all             postgres                                trust
local   all             all                                     trust

# IPv4 localhost connections - require password
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 localhost connections - require password
host    all             all             ::1/128                 scram-sha-256

# Docker network (172.x.x.x) - require password
host    all             all             172.16.0.0/12           scram-sha-256

# DENY all other connections - do not allow external access
# (This is implicit - anything not matched above is rejected)
